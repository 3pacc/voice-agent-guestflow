# Backend (FastAPI)

Ce dossier contient les manifests backend Python.
Le code applicatif reste dans `src/` pour l'instant.

## Installation

```bash
pip install -r backend/requirements.txt
```

## Run API

```bash
uvicorn src.main:app --host 0.0.0.0 --port 8000
```
